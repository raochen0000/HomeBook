{
  "name": "homebook-email-hook-fc",
  "version": "1.0.0",
  "private": true,
  "description": "家账 HomeBook · Supabase Send Email Hook 落地（阿里云 FC → 邮件推送 SingleSendMail HTTP API）",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "zip": "rm -f function.zip && zip -qr function.zip index.js package.json && echo 'wrote function.zip'"
  }
}

{
  "name": "homebook-push-fc",
  "version": "1.0.0",
  "private": true,
  "description": "家账 HomeBook · 系统推送投递（阿里云 FC 定时轮询 → Expo Push API）",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "poll": "node -e \"require('./index.js').handler({},{}).then((r)=>console.log(r)).catch((e)=>{console.error(e);process.exit(1)})\"",
    "test": "node test-send.mjs",
    "zip": "rm -f function.zip && zip -qr function.zip index.js package.json && echo 'wrote function.zip'"
  }
}

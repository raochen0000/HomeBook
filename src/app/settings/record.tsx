import { ComingSoon } from '@/features/settings/coming-soon';

/** G10 记账设置（默认类型 / 记一笔后行为 / 金额隐私）——下一轮以原生 SwiftUI Form + Toggle/Picker 落地。 */
export default function RecordSettingsScreen() {
  return <ComingSoon title="记账设置" note="记账设置即将上线" />;
}

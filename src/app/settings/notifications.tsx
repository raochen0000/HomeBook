import { ComingSoon } from '@/features/settings/coming-soon';

/** G12 通知设置（权限引导 + 分类开关）——下一轮以原生 SwiftUI Form + Toggle 落地。 */
export default function NotificationSettingsScreen() {
  return <ComingSoon title="通知设置" note="通知设置即将上线" />;
}

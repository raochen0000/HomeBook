import { ComingSoon } from '@/features/settings/coming-soon';

/** G16 意见反馈（写 Supabase feedback 表）——依赖建表，留待下一轮。 */
export default function FeedbackScreen() {
  return <ComingSoon title="意见反馈" note="意见反馈将在后续版本提供" />;
}

import { ComingSoon } from '@/features/settings/coming-soon';

/** G11 导出数据（CSV → 系统分享）——依赖 expo-sharing，留待下一轮（需重建 dev client）。 */
export default function ExportScreen() {
  return <ComingSoon title="导出数据" note="导出数据将在后续版本提供" />;
}
